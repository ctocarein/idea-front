/** Feature IAM (profil porteur) — édition du profil. Barrel. */
export { ProfileEditClient, type UserProfile } from "./ProfileEditClient";
export { updateProfile, type ProfileFormData } from "./profileActions";
